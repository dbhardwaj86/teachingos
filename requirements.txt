# Phase A (spine) uses only the Python standard library — nothing here is needed
# for `python -m samagra refresh|status|search`.

# Phase B (portal)
fastapi>=0.110
uvicorn[standard]>=0.29
jinja2>=3.1
python-dotenv>=1.0

# Phase C (lecture export) — pandoc binary must be on PATH for DOCX
google-api-python-client>=2.0
google-auth>=2.0
google-auth-oauthlib>=1.0

# Phase D (notifications)
requests>=2.31

# Phase D2 (LLM generation — the samadhan lane)
anthropic>=0.96
openai>=2.32  # LLM (Responses) + image (Images) API — Phase F1 figure lane reuses it, no new dep

# dev
pytest>=8.0
